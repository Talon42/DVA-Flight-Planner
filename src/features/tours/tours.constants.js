// Default derived-progress state used before Delta Virtual logbook data is loaded.
export const DEFAULT_DERIVED_TOUR_PROGRESS = {
  source: "deltava-logbook",
  lastSyncAt: null,
  tourProgress: {}
};
